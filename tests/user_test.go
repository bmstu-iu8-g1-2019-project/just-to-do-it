package tests

import (
	"testing"
)

func TestGetUserHandler(t *testing.T) {
	// Creates sqlmock database connection and a mock to manage expectations.
	//dbTest, _ := services.NewMockDB()
	//env := controllers.EnvironmentUser{dbTest}
}
