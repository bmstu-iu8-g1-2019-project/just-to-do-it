version: '3'
services:
  database:
    build: src/db/
    ports:
      - 5432:5432

  server:
    build: ./
    network_mode: host
    ports:
      - 5000:80
      - 5432:5432

    depends_on:
      - database
